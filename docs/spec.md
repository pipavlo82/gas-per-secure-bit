# ML-DSA-65 Verification Specification (Draft)

Defines:
1. Raw signature encoding (salt || w1 || w0 || z || hints)
2. Public key layout and optional NTT-expanded representation
3. Unified domain-separation format
4. Verification pipeline
5. Benchmarking methodology

This spec will evolve once Falcon-1024 comparison is performed.
